#!/usr/bin/env bash
# Script will create a back up of all MySQL databases
archive="$(date +%d-%m-%Y).tar.gz"
name=backup.sql
sudo mysqldump -uroot -p"$1" --all-databases > $name
echo "Backing up data $archive file, please wait..."
tar -cvzf "$archive" $name
